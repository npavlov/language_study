{
  "name": "language-study",
  "version": "1.0.0",
  "type": "module",
  "description": "Trilingual vocabulary game — learn English and Serbian with Russian hints",
  "scripts": {
    "predev": "node scripts/copy-db-assets.js",
    "dev": "vite",
    "prebuild": "node scripts/copy-db-assets.js",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/js/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npx playwright test",
    "parse": "node scripts/parse-words.js",
    "enrich": "node scripts/enrich-vocabulary.js",
    "migrate": "node scripts/migrate-to-sqlite.js",
    "vocab": "node scripts/vocab-cli.js",
    "verify": "node scripts/verify-translations.js",
    "fix-translations": "node scripts/apply-translation-fixes.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "better-sqlite3": "^12.6.2",
    "eslint": "^9.0.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.76.0",
    "sql.js": "^1.12.0",
    "xlsx": "^0.18.5"
  }
}
